# ---------------- MongoDB Atlas Connection ----------------
# Replace <username>, <password>, <cluster> with your Atlas credentials
MONGO_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/sosmechanics?retryWrites=true&w=majority

# ---------------- JWT Secret ----------------
# A long random string for signing tokens
JWT_SECRET=your_super_long_random_secret_here

# ---------------- Admin Seeding ----------------
# Allow bootstrap.js to seed the first admin and test jobs
ALLOW_ADMIN_SEED=true